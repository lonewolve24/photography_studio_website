{
    "$schema": "https://railway.app/railway.schema.json",
    "build": {
        "builder": "NIXPACKS",
        "buildCommand": "python -m venv --copies /opt/venv && /opt/venv/bin/python -m pip install -r requirements.txt"
    },
    "deploy": {
        "startCommand": "cd photostudio_backend && /opt/venv/bin/python manage.py migrate --no-input && /opt/venv/bin/python manage.py collectstatic --no-input && /opt/venv/bin/gunicorn photostudio_backend.wsgi",
        "restartPolicyType": "ON_FAILURE",
        "restartPolicyMaxRetries": 10
    }
}